#include "driver.hpp"
#include <iostream>

int main(int argc, char *argv[]) {
    // if (argc > 1)
    //     tensor_compiler::driver(argv[1]);
    std::cout << "Hello, Tensor Compiler!" << std::endl;
}
